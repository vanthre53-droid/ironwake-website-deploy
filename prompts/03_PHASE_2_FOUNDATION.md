# Phase 2 Prompt — Data, APIs, Auth, and Private Foundation

Resume only when G1, the P1.5 portfolio truth gate, and GS1 minimum social foundation are explicitly approved and their evidence hashes match. Read the approved architecture, real-data ledger, authority rules, security doc, verified social URL registry, and durable state.

Preserve an adequate existing stack. Otherwise use the approved current stable stack. Build typed content/claim/asset/offer registries, database schema/migrations, constraints, indexes, RLS/authorization, server validation, rate limits, abuse controls, audit logging, idempotent APIs/server actions, outbox/retry/dead-letter foundation, secure owner auth, recovery/MFA path, and the authorized private dashboard shell.

Do not build public success states or send external notifications. Use safe fixtures only. Secrets are entered by the human directly into encrypted environment settings and never pasted into files/chat.

Run Gate B tests, including unauthenticated, wrong-role, cross-object, expired-session, duplicate request and validation failures. Store exact evidence and update every durable state file. Stop for G2 if migrations/auth/retention decisions differ materially from G1.
