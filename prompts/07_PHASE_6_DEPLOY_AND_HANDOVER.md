# Phase 6 Prompt — Production, Real Test, Observation, Handover

Resume only when G5 explicitly approves the exact commit and preview. Deploy only that commit through the approved owner/project.

Verify production DNS/TLS/headers/routes/robots/canonicals/sitemap/structured data/auth/provider endpoints. Run one approved real inquiry and booking request with approved contact data. Verify one durable CRM record, correct appointment state, task/timeline, and exactly-once approved owner email/WhatsApp; verify customer acknowledgement and provider callbacks where enabled. Delete/anonymize the test according to G6 and the retention policy. Trigger and receive a monitoring alert.

Observe the approved window, document failures and fixes, then produce the full handover package in `docs/07_TEST_RELEASE_AND_HANDOFF_GATES.md`. Mark each feature `LIVE`, `TESTED DEMO`, `PROVIDER PENDING`, or `DEFERRED`. Report exact evidence and limitations; never claim bug-free, fully secure, military-grade or guaranteed ranking.

Finish by updating the durable state, evidence index, work log, and CLI handoff with maintenance, renewal, monitoring, rollback, and the next 30-day measurement action.
