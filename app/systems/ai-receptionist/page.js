import { AiReceptionistSystem } from './AiReceptionistSystem';

export const metadata = {
  title: 'AI Receptionist (Concept) — IronWake',
  description: 'A disclosed, human-supervised first-response concept for phone and chat reception. No phone line, voice provider, or pricing is connected yet.'
};

export default function AiReceptionistPage() {
  return <AiReceptionistSystem />;
}
