import { AuditForm } from './AuditForm';

export const metadata = {
  title: 'Business Leak Audit — IronWake',
  description: 'Request a free review of where a real inquiry can disappear between enquiry, booking, and follow-up. No booking, quote, or provider connection is implied.'
};

export default function AuditPage() {
  return <AuditForm />;
}
