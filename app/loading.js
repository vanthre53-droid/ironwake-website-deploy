export default function Loading() {
  return <main className="shell"><section className="loading-state" aria-live="polite" aria-busy="true"><span className="eyebrow">IronWake</span><h1>Preparing the next view.</h1><p>The page is loading. No inquiry, booking, or provider action is happening in the background.</p><span className="loading-line" aria-hidden="true" /></section></main>;
}
