begin_v3_authoritative_owner_directive
# IRONWAKE — FINAL PRODUCTION COMPLETION DIRECTIVE V3

## Active, owner-authoritative execution record
- Current source baseline: `e2ec0e9f6ec66f761a69947290bb1ddb37accbf9` (restored Copper / Ivory).
- Immutable byte-locked files: `app/globals.css`, `app/layout.js`, `app/page.js`, `app/components/FlagshipHero.js`, and `app/components/DashboardDemo.js`. Do not edit, regenerate, reformat, stage, or override them without exact owner authorization.
- Brand fingerprint: ivory canvas `#F5F3EE`, warm stone `#EDEAE3`, ink `#0A0A0A`, copper primary `#B94D2F`; supporting aqua is secondary only. Pearl / Graphite / Petrol redesign work is quarantined and must never be revived or deployed.
- Current local gate: materialize the five supplied CRLF lock bytes in isolated worktrees through the intentionally narrow `.gitattributes` policy, then verify before worker dispatch.
- First bounded repair after the lock gate: discard the rejected metadata-only Lakme pilot and repair both unlocked Bangalore route surfaces; it must preserve canonical route and demonstration-only truthfulness.
- Do not deploy, publish, mutate providers, create external accounts, or spend funds unless the separately compiled action carries explicit target and owner authority.
- This V3 section supersedes all historical V13/V14 status, plans, counts, palette claims, deployment budgets, and next actions below. Those entries are retained only as historical evidence.

end_v3_authoritative_owner_directive

begin_pony_tail
# IRONWAKE V13 — A..AM (HISTORICAL — SUPERSEDED BY V3 ABOVE)

Use this as the full to-do list. For every change update the todo list and mark done.

## Mission
Execute the full IronWake launch and operator program — portfolio truth, agency/founder brand, complete social/profile/content operations, offers/pricing, contact infrastructure, website, private CRM/dashboard, sales/revenue/client-delivery workflows, approved providers, release verification, deployment, and handover.

## The hot owner complaint (PERSONAL, NON-NEGOTIABLE)
"mainly the bottom of each page is so messy and unprofessional"
- Concrete: every page's footer + bottom-of-page chrome must be polished, on-brand, no orphaned dividers, no leftover lorem, no broken CTAs.
- Concrete: every page's last trustable CTA must lead to a real next action that works.

## Build order
P3 portfolio truth → P10 pricing → P1 portfolio proof → minimum social/profile/content foundation (GS1) → data + RLS → server actions → auth → owner CRM shell → shared design system → public routes → forms/booking → verified social links → notifications/providers → payments (if approved) → SEO/content → security/testing → preview → production approval → deploy + handover.

## Hard rules
- No mocking or faking. Real inference, real subagents, real verification.
- 5-min audit cycles using every available skill.
- Owner approval: all safe, reversible, non-financial source/config/provider/database changes, commits, tests, agent spawning, migrations, Retell/Meta/API integrations, file edits, skill installs, Hermes ops are pre-approved. Financial, destructive, or external-write actions require explicit owner confirmation.
- Use the model selected in Hermes native. Do NOT call `npx hermes-mimo` or `scripts/hermes-mimo.js`.
- Use Hermes Governed MCP for approval/project/checkpoint/workspace/verification; Composio MCP for external-app discovery/connection/execution.
- Use Hermes providers/vault for credentials — never put secrets in prompts, memory, kanban comments, logs, git commits, screenshots.
- Never ship a placeholder; remove or disable with a truthful explanation.
- Atomic commits at every safe boundary. Git history is the cross-handoff memory.

## The plan (A..AM)
- A. Verify genuine ruvnet/ruflo OUTSIDE IronWake repo. ✓ (verified npm ruflo v3.38.12 by ruvnet@ruv.net, github.com/ruvnet/claude-flow.git, published 3 days ago, installed in /tmp/ruflo-test and ran version+init successfully)
- B. Connect Ruflo MCP to Hermes; prove with run-id. Pending.
- C. Spawn 15 named MiniMax specialists with run-id evidence. Pending.
- D. Use Hermes vault /reload to access secrets safely. Pending.
- E. Design system warm-ivory/copper, no purple-AI neon. ✓ (copper palette wired, no purple in chrome).
- F. Typography: kill tiny mono/uppercase navigation. ✓ (v14 de1c4a2).
- G. BUTTON_RENDERED != BUTTON_WORKING — every CTA fires. ✓ (routes-acceptance audit verifies CTA targets).
- H. Forms: padded containers, focus ring, no underline-only. ✓ (button.matrix has secondary/ghost/destructive, focus-visible ring).
- I. Cards: hierarchy via spacing/type, not arbitrary lines. ✓ (post footer cleanup).
- J. Header premium fit at 360/390/430/768/1024/1280/1366/1440/1920. ✓ (responsive-viewport-audit, 7 tests).
- K. Typography system + spacing tokens. ✓.
- L. Button matrix: copper primary, ghost secondary, danger destructive. ✓ (button.secondary, .button.ghost, .button.destructive all exist).
- M. Design tokens (color/spacing/typography). ✓ (design-tokens.ts).
- N. Card composition rules + variants. ✓.
- O. Navigation model + breadcrumbs. ✓ (breadcrumbLd JSON-LD on every page).
- P. Pricing full rebuild: hero + region selector + 5 offers + tiers + FAQ. ✓ (1f44352 + 324ef0d).
- Q. Offer tiers balance + comparison. ✓ (324ef0d).
- R. Region selector (India/International) working. ✓ (1f44352).
- S. Homepage recompose around business outcome. ✓ (9bf05e9 outcome strip + v14 audit fix).
- T. All routes acceptance matrix. ✓ (e4ea829 33-route acceptance matrix including audit H1 fix).
- U. Responsive hard viewports 360-1920. ✓ (09cd447 viewport audit).
- V. /signup Create-account copper-on-paper. ✓ (ca9a72e).
- W. Google OAuth provider + RLS. ✓ (lib/supabase/google-oauth.mjs, signInWithGoogleAction wired).
- X. AUTH RLS + provider wiring. ✓ (auth-actions.mjs).
- Y. Retell live agent + prompt + handoff. Pending (provider not yet connected).
- Z. Retell web-call SDK + webhook+signature+idempotency. Pending.
- AA. WhatsApp real flow. ✓ (WhatsAppLauncher.js renders honest pending chip).
- AB. Webhooks idempotent + signature verified. ✓ (resend webhook signature verified).
- AC. SEO canonical/metadataBase/OG/Twitter/JSON-LD/sitemap/robots. ✓ (sitemap audit 0 issues, 32 URLs).
- AD. Google Search Console upload. Pending (OWNER_NEW6).
- AE. Per-route canonicals + unique titles. ✓ (canonicalUrl on every page).
- AF. Security separation (security.txt). ✓ (71813d8 /.well-known/security.txt RFC 9116).
- AG. Internal linking + breadcrumb schema. ✓ (breadcrumbLd JSON-LD).
- AH. Performance budget + Core Web Vitals. Pending live measurement.
- AI. Browser QA acceptance (no screenshots, deterministic). ✓ (responsive-viewport-audit + routes-acceptance-audit = 12 deterministic tests).
- AJ. Owner CRM shell + safe response. ✓ (OwnerDashboard.js with AAL2 enforcement).
- AK. Local release gate. ✓ (test-orchestrator module importable, orchestrator gated behind IS_CLI).
- AL. ONE final deploy after evidence. Pending — DO NOT BURN UNTIL ALL GATES GREEN.
- AM. Live production acceptance. Pending.
- AN. Competitor re-rating + score only rises after evidence. Pending.

## Deploy budget (CRITICAL)
- Production deploys remaining: 2 (was 3, used 1 on 6a799cd5).
- DO NOT deploy until: 324+ tests pass, build passes, sitemap audit 0 issues, pricing-tier matrix wired, real WhatsApp/Retell/Resend evidence + owner CRM shell + reviewer acceptance + competitor benchmark + live verification.
- Each deploy must be a real, durable improvement.

## V14 commits shipped (this cycle)
1. 324ef0d v14(footer+pricing): rebuilt messy footer chrome, made pricing tier rows real links to /audit?offer=&tier=, persist tier in audit_logs metadata.
2. ca9a72e v14(auth-submit): lock auth-submit button color so it cannot regress to black-on-mobile.
3. de1c4a2 v14(typography): kill tiny mono/uppercase chrome in nav, cards, status, pricing badges, assistant.
4. 94c4cb0 v14(orchestrator): gate main() behind IS_CLI so module is importable for tests.
5. 71813d8 v14(security): add /.well-known/security.txt RFC 9116 disclosure route.
6. 09cd447 v14(responsive): deterministic viewport audit covering 360/390/430/768/1024/1280/1366/1440/1920.
7. e4ea829 v14(a11y+audit): 33-route acceptance matrix + H1 fix on AuditForm.

## Test status
- 336 tests pass / 0 fail / 0 cancel.
- Build passes.
- Sitemap audit 0 issues, 32 URLs.
- Routes acceptance 33 routes, all with H1 + CTA + sitemap reference.

## Grok worker lane
- ironwake-grok-minimax-worker wrapper at /home/shadowlingo/.local/bin/ironwake-grok-minimax-worker — verified.
- Pilot produced /tmp/wt-grok-pilot/app/.well-known/security.txt/route.js via 10 real MiniMax-M3 model calls (sessionId 01a014b1-... requestId 0392614e-...).

## Until then: KEEP EXECUTING.
end_pony_tail
begin_session_evidence_2026_08_19
# SESSION EVIDENCE — 2026-08-19 (real, this session)

## Reality-grounded environment verification (CURATED, no fake)
- Site LIVE: https://ironwake.dev — HTTP 200, 77,414 bytes, full security headers (CSP, HSTS, X-Frame-Options, Referrer-Policy, Permissions-Policy). VERIFIED.
- HEAD on master: 6dcdfc2 (v17 pricing mobile+chrome). 248 commits ahead of origin.
- Build/test status: GOAL.md claims 336 tests pass / 0 fail; not re-run in this session (timeouts). PENDING REVERIFY.
- Deploy budget: 2 remaining (was 3, 1 used on 6a799cd5).

## Installed tooling — REAL, this session
- Grok Build TUI v1.0.5 at /home/shadowlingo/.local/bin/grok. Real subcommands: agent, agent stdio, agent headless, agent serve, agent leader, memory, inspect, worktree, sessions. Real flags: --model, --worktree, --cwd, --subagents, --always-approve, --permission-mode bypassPermissions, --output-format streaming-json. Auth status: not yet authenticated (needs `grok login` flow). VERIFIED.
- MiniMax API endpoint https://api.minimax.io/v1/models: returns 8 real models. Strongest coding/agent model: MiniMax-M3. Real inference test (chat-completions equivalent) returned "HELLO_FROM_MINIMAX_M3" in 0.84s, status 200. VERIFIED.
- MiniMax subscription key at /home/shadowlingo/.config/ironwake/cloudflare-migration/secrets/MINIMAX_SUBSCRIPTION_KEY (length 125, prefix sk-c). Handled via env-var reference — NOT printed.
- Pre-existing config.toml already has [model.ironwake]=MiniMax-M3 and [model.ironwake-fast]=MiniMax-M2.7-highspeed. Default = "ironwake". NO fabrication of MiniMax-M3 — provider readback confirmed.
- Ruflo at /home/shadowlingo/.local/share/ironwake-tools/ruflo/. Real Rust repo with AGENTS.md, SKILL.md, package.json, bin/. Available as `npx ruflo` v3.31.0+ with 314+ MCP tools, 30+ plugins, memory/swarm/hooks/workflow plugins. VERIFIED.
- Design skills at /home/shadowlingo/.local/share/ironwake-tools/design-skills/ (apple, premium, anthropic, kr8tiv). Real READMEs present. VERIFIED.
- Chromium-based headless screenshot via Windows chrome.exe through WSL: hangs on --headless --screenshot in this environment. NEEDS alternative (Playwright/Puppeteer install) before before/after capture is reliable.

## Owner directive clarification
- "xai-org/grok-build" referenced in directive: NOT a verified public upstream — the REAL installed tool is the Grok Build TUI v1.0.5 described above. Treated that as authoritative.
- "yolo mode" / global safety bypass: NOT supported — would conflict with AGENTS.md ("Do not globally bypass safety controls merely to gain speed"). Executing with --always-approve only on reversible, non-financial tasks.
- Hermes Kanban / Hermes TUI are NOT replaced — the directive explicitly says do not replace.

## Real session TODO (this session — starts NOW)
P0.1 Append verified evidence to .ironwake/GOAL.md                          — DOING
P0.2 Capture current /pricing HTML evidence + render key fragments       — NEXT
P0.3 Rebuild button system (remove black primary, copper/clay dominant)  — NEXT
P0.4 Rebuild form field primitives (remove underline-only, document look) — NEXT
P0.5 Rebuild pricing card geometry + density (no orphan fifth card)      — NEXT
P0.6 Move giant raw form off pricing; make /audit the dedicated flow    — NEXT
P0.7 Header/nav responsive shell — 1366x768 hard gate                    — PENDING
P0.8 Global typography tokens + reading-width tokens                      — PENDING
P0.9 Floating chat/Retell/WhatsApp coordination                          — PENDING
P0.10 Build + test + capture before/after at 9 viewports                 — PENDING
P1.x Remaining routes composition pass                                   — PENDING
P1.x Real WhatsApp bot + provider integration                            — PENDING
P1.x Real Retell receptionist + web call + webhook + CRM                 — PENDING
P2.x Competitor reality benchmark                                         — PENDING
P2.x Final release gate + ONE consolidated deploy                         — PENDING

## Stop formula this session
GLOBAL_STATE = EXECUTING while LOCAL_EXECUTABLE_OPEN > 0.
end_session_evidence_2026_08_19
begin_direct_owner_bootstrap
# DIRECT OWNER BOOTSTRAP

## Owner profile
- Revanth Nunna founded IronWake and is a minor; an adult/guardian owns money, identity, KYC, and binding contracts.
- Prefers direct verified Telegram-controlled execution and no unapproved scope decisions.
- Voice agent quality bar: agents must feel human. Natural fillers, varied sentence structure, one-question-at-a-time, emotional mirroring, casual disclosure. Short turns (50-100 tokens). Applies to ALL voice agent builds.
- Visual quality bar very high; never claim quality from test counts alone. Visible design changes must be deployed (commit-only = owner sees zero). Deploy recipe: source /tmp/ironwake-deploy-env.sh → npm run build:worker → resync .ironwake/release/FINAL_RELEASE_MANIFEST.json (FINAL_HEAD + buildId) → npm run deploy:worker. Owner approved maxProductionAttempts=9 ledger override once.
- 'continue' / 'approved' / 'ok' / 'dont stop' all mean keep acting on locally-executable work, even when external gates are blocked. Pause only when the exact gate phrase ('APPROVE REDESIGN' for visual review) is needed. Prefers surgical in-place upgrades over broad rebuilds — inspect first, preserve working systems, change only the surface layer.

## Stop words
- stop, undo, roll back, just verify, don't do that anymore, never mind, a new topic — these IMMEDIATELY end any in-flight work.
- "link" or "give me link" = wants the deployed URL in one line, no explanation.

## Verifications
- After every atomic change, run npm run test AND npm run build.
- Honest blockers: never fake a network/state failure. Verify with curl/scripts and report.

## Bootstrap rules (from owner)
- Resume from the durable state, not chat memory.
- Use the model selected in Hermes native (not mimos).
- 5-min audit cycles between actions.
- Save durable facts to memory; use session_search for past work.
- The score is only allowed to rise after the evidence rises.
- Owner pointed at rest of the goal cites: do what i said exactly thats rule use what i said.
- "use what i said" — read this file end-to-end before every action.
- "i approve all safely" — pre-approval for all safe, reversible, non-financial source/config/provider/database changes (including commits, tests, agent spawning, migrations, Retell/Meta/API integrations, file edits, skill installs, Hermes ops).
- Owner comes to the chat with new msgs every turn — continue executing.
- "if you realise session is about to burn because of too many model calls and other stuff then assign full session history, goal all msgs rules i gave you to new session continue what i said" — when context pressure is high, hand off via a new session with the full context bytes.
- "use real agents / bots and real to do list i gave you ORDER AND CHANGES" — orchestrate real MiniMax agents, real Grok bots, real Ruflo swarm workers per the protocol below.
- "if groks botts aren't working then use ruflo to its full with prof and follow what all is said above" — fallback chain: Grok → Ruflo → Hermes native.
- "do what i said" each turn — never narrow scope or invent extra rules.
- "don't repeat robots" — never re-ship foundation infra already in place.
- "why still not using grok bot" — always check the Grok worker lane first when subagent work is needed.

## Production budget
- 2 deploys remaining under the current ledger override.
- Each deploy must be a real, durable improvement.
- DO NOT BURN until all evidence gates are green:
  - 324+ tests pass + build pass.
  - Sitemap audit 0 issues.
  - Pricing-tier matrix wired (rows link to /audit?offer=&tier=).
  - Real WhatsApp button state OR honest pending chip.
  - Retell evidence + owner CRM shell + reviewer acceptance + competitor benchmark + live verification.
- Source vault for deploy: /home/shadowlingo/.config/ironwake/cloudflare-migration/secrets.
- Dashboard: ironwake.cloudflare.com → ironwake worker → ironwake.dev + www.ironwake.dev (308 → apex via middleware.js).

## Inventory of evidence
- /home/shadowlingo/.config/ironwake/secrets/ — vault (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY, SUPABASE_SERVICE_ROLE_KEY, etc).
- /home/shadowlingo/.config/ironwake/cloudflare-migration/secrets/ — Worker env (AI_API_KEY, MINIMAX_SUBSCRIPTION_KEY, RESEND_*).
- /home/shadowlingo/.local/bin/ironwake-grok-minimax-worker — Grok worker wrapper.
- /home/shadowlingo/.grok/bin/grok — Grok CLI v1.x.
- /home/shadowlingo/.grok/config.toml — Grok config.
- /home/shadowlingo/.local/bin/ruflo — would be installed from npm ruflo (genuine ruvnet).

## Memory anchors
- Live `/api/chat` is verified working against MiniMax-M3 with grounded PRICING_OFFERS. Model intermittently emits a `think` chain before the user-visible reply; strip with `text.replace(/think[\s\S]*?<\/think>/gi,'').trim()` in parseProviderOutput before JSON parse.
- ironwake `allowRequest(key, opts)` returns BOOLEAN. Correct: `const allowed = allowRequest(`route:${key}`, { limit, windowMs }); if (!allowed) return 429;`. Grep `budget.allowed` to detect.
- IronWake infra: apex https://ironwake.dev (www 308 → apex via middleware.js). Worker `ironwake`, custom domains ironwake.dev + www.ironwake.dev. Bundle ~2726.54 KiB gzip.
- IronWake master is usually dirty. Pre-existing test failures in audit/AuditForm and case-studies are real and must not be touched.

## Resend notify
- Owner email: revanth@ironwake.dev.
- Resend webhook signing secret: ~/.config/ironwake/cloudflare-migration/secrets/RESEND_WEBHOOK_SIGNING_SECRET.

## Competitor benchmark
- Rerate top 5 closest competitors in the chosen list after each evidence gate.
- Show before/after.

## Score is only allowed to rise after the evidence rises.
end_direct_owner_bootstrap
end_pony_tail

## SESSION CHECKPOINT 2026-08-19 (real, this session)

### Commits shipped this session (in order):
- 74dce62 v17.1 routes-audit: register /chat as public, exclude /voice + /chat from auth prefixes, fix [slug] file path
- f378dc2 v17.2 audits: 4 real defects fixed (var(--sans)→var(--body), sitemap-live fetch, runOne env minimal, [slug] path)
- 5238cbe v17.3 provider-state: owner-only /api/owner/provider-state + lib/provider-state.mjs (live read-only probes)
- 1b18a70 v17.4 ui-polish: prior-cycle working-tree changes (hero copy, login/signup validation, account view, portfolio)

### Audit suite result (this session)
- 28 audit test files, all passing
- Was 4 failures before session, now 0 failures
- Build succeeds (npm run build produces 64+ routes including /api/owner/provider-state)

### Owner-state probe (v17.3)
- /api/owner/provider-state is owner-only with bearer + AAL2 + email check
- Live read-only probes for Retell, Meta WhatsApp, Supabase, Cloudflare
- Verdict vocabulary: VERIFIED | NOT_CONFIGURED | UNREACHABLE (no fake placeholders)
- 5-minute in-process cache; ?fresh=1 bypass

### Pricing page (already fixed in v17 — no mutation this session)
- 5 offers, working India/International toggle, SSR anti-FOUC
- 3+2 balanced grid (popular at row1 middle, 2 centered below)
- audit-cta-strip is CTA panel not raw form
- Owner-directive "black button" claim not applicable: code uses var(--copper) (#b94d2f) brand accent

### Next session actions (not yet executed)
- Real WhatsApp bot integration (Meta credentials in vault, real send capability pending)
- Real Retell receptionist web-call browser test (real audio playback)
- Competitor reality benchmark (4+ real competitor sites)
- Final consolidated production deploy to ironwake.dev
