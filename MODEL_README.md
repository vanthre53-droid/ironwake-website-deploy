# MODEL_README — IronWake

## HEADER
- Goal: Deliver a truthful IronWake website and operator system from approved sources and evidence.
- Client / niche: IronWake service-business operations agency.
- Started: 2026-07-26
- Workspace: /mnt/c/Users/vanth/Downloads/ironwake
- Owner skills: graphify, verification, model-router, coordination.

## STATE
- Phase: C1 correction complete; M1 is active at W04.
- Done: C1 reconciled the two Stitch sources and W03 Instagram ownership/security verification is human-attested.
- NEXT SINGLE ACTION: Human applies `reports/M1_W04_INSTAGRAM_PROFILE_EDIT_PACKET.md` in Instagram and confirms logged-out avatar/bio/contact-CTA readback; M1 then completes W04.
- Blockers: W04 manual execution because the discovered API/browser cannot edit profile fields; full GS1 and G3 block public W11 implementation.

## HANDOFF
Read these first, in order:
1. state/PROJECT_STATE.yaml — active role, gate, and exact action.
2. reports/C1_VISUAL_SOURCE_RECONCILIATION.md — settled Stitch-source decision.
3. state/SEALED_TASK_QUEUE.yaml — immutable M1 work boundary.
4. reports/SCREEN_ROUTE_MATRIX.md — truthful disposition for all 30 screens.

Context a stranger needs:
- The separate Stitch design ZIP is the visual-token authority: warm ivory, ink, copper, Manrope, Inter, JetBrains Mono, 12-column layout, restrained light-glass surfaces.
- The 30-screen Stitch archive is the route, composition, interaction-state, and information-architecture reference.
- `app/` is the single Next.js runtime. `website/` is a duplicate prototype to retire only after parity evidence.
- Preserve the verified audit API, validation, migration, and tests; do not extend the lime diagnostic-console styling.
- G1 and demonstration-only G1.5 are approved. GS1 and G3 remain pending; no public visual implementation, social publication, provider action, or deployment is authorized.
- No secrets are stored in this repository; do not read `.env.local` values.

Open questions for the human:
- Apply the approved Instagram profile packet manually and confirm logged-out avatar/bio/contact-CTA readback.

## ROUTING LOG
SESSION: 2026-07-30T13:09:10Z model=gpt-5.6-terra read_handoff=created_after_session_start
ROUTE: class=architecture tier=strong model=gpt-5.6-terra reason="bounded cross-source C1 reconciliation" step="C1 visual-source reconciliation"
ROUTE: class=implementation tier=mid model=gpt-5.6-terra reason="sealed M1 social ownership evidence update" step="M1 W03 Instagram attestation"
ROUTE: class=integration tier=mid model=gpt-5.6-terra reason="approved Instagram profile edit with capability discovery" step="M1 W04 Instagram profile packet"

## VERIFY LOG
VERIFY: step="C1 visual-source reconciliation" gate=runs+schema+contains result=PASS attempts=1
VERIFY: step="M1 W03 Instagram attestation" gate=contains+state-validation+secret-scan result=PASS attempts=1
VERIFY: step="M1 W04 Instagram profile packet" gate=provider-readback+schema+browser-preflight+secret-scan result=PASS attempts=1

## DECISIONS
- 2026-07-30 — Use the external Stitch design ZIP for visual tokens and the 30-screen archive for composition/route reference — because the current implementation matched neither source and the external design system is the approved token source.
- 2026-07-30 — Keep `app/` as the sole runtime; retire `website/` only after verified parity — because duplicate implementations cause drift.

## FILES
- app/ — Next.js runtime and audit intake UI/API.
- website/ — legacy private static prototype; not the target runtime.
- supabase/ — reviewed inquiry migration.
- reports/ — evidence, source audits, and C1 reconciliation.
- state/ — durable role, queue, scope, evidence, and handoff records.
