# IronWake Real Capability Ledger

- Programme: `IRONWAKE_REAL_PRODUCT_COMPLETION`
- Audit phase: `P0.1 REAL CAPABILITY AUDIT`
- Audited by: Codex
- Audited at: 2026-08-09 UTC
- Repository HEAD at this checkpoint: `83b0b48e91feed351798f08d0c422cf7036ad26d`
- Deployed application commit: `daafc01a7bf23cf1168bdaac6c285f621f5f3449`
- Production: `https://ironwake-site.netlify.app`
- Rule: this ledger is reconstructed from current source, live provider/database readback, and deployed HTTP behavior. The prior `VERIFIED_COMPLETE` flag and matrix are historical evidence only.
- Remediation checkpoints: live Supabase migrations `20260809101715_secure_owner_and_privileged_rpcs` and `20260809104514_durable_notification_state_machine` are verified on 2026-08-09. The fail-closed Resend adapter/worker, raw-body signature-verified delivery webhook, and owner notification operations are locally implemented and tested but unconfigured and undeployed. Authorization and durable notification state are repaired; provider execution and the overall programme remain `PARTIAL`.

## Status vocabulary

`MISSING`, `IMPLEMENTED_ONLY`, `CONFIGURED`, `CONNECTED_NOT_VERIFIED`, `VERIFIED_LIVE`, `FAILED_LIVE`, and `INTENTIONALLY_DEFERRED_NOT_PUBLICLY_CLAIMED` have the meanings fixed by the current owner programme. `Yes*` means the claim is qualified or contradicted elsewhere in the public copy. `Partial` never means live completion.

## Capability inventory

| Capability | Publicly claimed? | Code exists? | Real provider required? | Provider configured? | Provider connected? | Database side effect? | External side effect? | Owner can observe it? | Failure observable? | Retry exists? | Live E2E verified? | Status | Current evidence / mismatch |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|---|
| Public website routes | Yes | Yes | Netlify | Yes | Yes | No | Page delivery | Yes | HTTP status | Platform-managed | Yes | `VERIFIED_LIVE` | Current production GETs for `/`, `/pricing`, `/audit`, `/book`, `/owner`, `/admin`, and three system pages return 200. |
| Repeatable Git-linked production deployment | Internally required | Config only | Netlify + GitHub | No | No | No | Deployment | Partial | Partial | Platform-managed | No | `MISSING` | Netlify project is live, but `getSite` exposes no linked repository settings and Netlify reports zero configured environment-variable names. The current deploy is a manual deploy carrying Git commit metadata, not proof of a reproducible Git-linked build. |
| Regional pricing presentation | Yes | Yes | No | N/A | N/A | No | No | Yes | N/A | N/A | Yes | `VERIFIED_LIVE` | Production/source contain the locked 5 offers × 3 tiers × India/International schedules. No FX conversion or sixth package exists. |
| One canonical pricing source | Yes* | Yes, local | No | N/A | N/A | No | No | N/A | N/A | N/A | No | `IMPLEMENTED_ONLY` | `lib/pricing.mjs` now owns the approved five offers, three tiers, two regional schedules, Lite-price helpers, and SEO summary. The pricing page, system references, deterministic guide, homepage, and pricing structured data read it; 143 local tests pass. Production still serves an earlier artifact, so the source consolidation is not yet live. |
| Portfolio demonstration links | Yes | Yes | Vercel per demo | Yes | Yes | No | Remote demo delivery | Yes | HTTP status only | Platform-managed | Yes | `VERIFIED_LIVE` | All nine current Vercel URLs returned HTTP 200 in this audit. This proves reachability only, not clients, outcomes, providers, or uptime. |
| Supabase CRM persistence core | Yes | Yes | Supabase | Yes | Yes | Yes | No | Partial | API/log error only | No | Yes | `VERIFIED_LIVE` | Live database still contains 38 inquiries, 29 contacts, 36 consents, 36 review tasks, 36 outbox intents, and 44 audit-log rows after the notification migration. RLS remains enabled on the six core private tables; the two new notification evidence tables are also RLS-enabled and empty. |
| Public audit intake persistence | Yes | Yes | Supabase | Yes | Yes | Yes | No | Partial | Safe 4xx/5xx + server log | Browser retry creates another inquiry | Yes | `VERIFIED_LIVE` | Deployed route rejects invalid input; live rows and the deployed RPC prove durable intake. Full notification/customer workflow is separate and not live. |
| Consent recording and withdrawal | Yes | Yes, local owner control | Supabase | Yes | Yes | Live canonical-owner RPC for withdrawal; no withdrawal invoked | Local owner status/control; deployment pending | Explicit owner confirmation | Safe RPC failure | Idempotent; cancels only unsent customer notifications | Partial | `PARTIAL_LIVE_SCHEMA_LOCAL_UI_DEPLOY_PENDING` | Current intake RPC atomically creates a `contact` consent for new inquiries. Migration `20260809153000_owner_consent_withdrawal` adds an authenticated-only, security-definer RPC that rechecks the canonical owner, marks contact consent withdrawn, clears the next follow-up, cancels only queued/retry-scheduled customer notification intents, and writes metadata-only audit evidence. The local owner UI asks for confirmation and invokes only that RPC. No consent was withdrawn during implementation; the UI remains undeployed and unexercised by an owner session. |
| Shared production rate limit | Yes* | Yes, local route | Supabase durable store | Yes | Yes | Yes, live RPC | No | No | Safe 429/503 responses after deployment | No | No | `IMPLEMENTED_ONLY` | Live Supabase contains an RLS-protected, service-role-only atomic counter RPC with bounded inputs and hashed keys. The local audit route calls it and fails closed on storage failure; the deployed route remains `daafc01`, so public multi-instance enforcement is not yet proven. |
| Honeypot / server validation | Yes | Yes, local hardening | No | N/A | N/A | Prevents DB write | No | No | Safe 400/413/415 responses | N/A | Partial | `IMPLEMENTED_ONLY` | The repaired local route accepts only exact `application/json` media types, bounds declared and actual bodies, rejects malformed JSON before dependencies, and rejects ASCII control characters in business/email/leak/source while accepting ordinary Unicode. Targeted negative tests pass; deployed `daafc01` cannot yet prove these additions. |
| Booking-request classification | Yes | Yes, local UI | Supabase | Yes | Yes | Yes | No | Local owner view; deployed view pending | Safe API result + durable status | No | Partial | `IMPLEMENTED_ONLY` | Live DB has four `website_booking` inquiries, now backfilled to `REQUEST_RECEIVED`. New requests atomically persist source, request status, next action, due task, audit event, and distinct owner/customer notification intents. The local UI says `BOOKING REQUEST RECEIVED` and does not claim confirmation; deployment and real notification proof remain pending. |
| Booking state machine | Yes* | Yes, request-only | Optional calendar for provider confirmation | No | No | Durable `booking_status` field | No | Local owner view | Safe state / request-only copy | No | No | `IMPLEMENTED_ONLY` | Live forward migration defines `REQUEST_RECEIVED`, `REVIEWING`, `CONFIRMED`, `NEEDS_FOLLOW_UP`, `CANCELLED`, and `FAILED`. Only `REQUEST_RECEIVED` is exercised; no calendar provider, reservation, or `CONFIRMED` claim exists. |
| Calendar/provider booking integration | Disclosed as pending | No | Calendar provider | No | No | No | No | No | No | No | No | `INTENTIONALLY_DEFERRED_NOT_PUBLICLY_CLAIMED` | Request-only public copy is truthful about no confirmed booking. |
| Owner transactional email | Yes | Yes, local | Transactional-email provider | No | No | Durable owner intent/attempt state | No | No | Safe worker result | Bounded local/DB contract | No | `IMPLEMENTED_ONLY` | Owner audit/booking/priority templates, the Resend adapter, inline best-effort worker, and scheduled entrypoint exist locally. Configuration fails before claim, all tests use fakes, and no account/key/send/deployment exists. Public “instant email notifications” wording remains unsupported. |
| Customer audit acknowledgement email | Internally required | Yes, local | Transactional-email provider | No | No | Durable customer intent/attempt state | No | No | Safe worker result | Bounded local/DB contract | No | `IMPLEMENTED_ONLY` | Escaped plain-text/HTML acknowledgement templates and customer routing exist locally. No provider is configured and no message was sent. |
| Customer booking-request acknowledgement email | Internally required | Yes, local | Transactional-email provider | No | No | Durable booking-received intent/attempt state | No | No | Safe worker result | Bounded local/DB contract | No | `IMPLEMENTED_ONLY` | The local customer template uses exact `BOOKING REQUEST RECEIVED` wording and explicitly denies confirmation. It has only fake-adapter proof; no transactional message was sent. |
| High-priority lead alert | Internally required | Yes, local | Email provider | No | No | Durable priority intent | No | No | Safe queue/worker result | Bounded local/DB contract | No | `IMPLEMENTED_ONLY` | The intake route queues one idempotent priority intent only for high, urgent, or `needs_human` triage and then invokes the shared worker best-effort. No live provider send or escalation evidence exists. |
| Notification outbox intent | Yes | Yes | Supabase | Yes | Yes | Yes | No | Read-only admin list if authorized | Durable state | DB contract only | No | `CONNECTED_NOT_VERIFIED` | The live intake RPC definition queues distinct owner/customer audit or booking intents atomically. The 36 historical undifferentiated rows are safely cancelled as non-addressable legacy events; no new synthetic inquiry was created to exercise the revised definition. This remains intent, not delivery. |
| Notification idempotency | Yes | Yes | Provider-specific support | No | No | Event, attempt, provider-message, and provider-event uniqueness | No | Partial | Durable safe state | DB plus adapter/callback contract | No | `IMPLEMENTED_ONLY` | Deterministic event keys and database uniqueness are live; the local Resend adapter passes that same key through the current SDK `idempotencyKey` option, while signed callback fixtures prove durable provider-event replay deduplication. No provider request has exercised the 24-hour provider window. |
| Notification worker / bounded retry / dead letter | Yes | Yes, local | Email provider + executor | No | No | Claim/finish/retry/dead-letter RPCs | No | Read-only status text | Safe result/codes | 5m then 30m; max 3 | No | `IMPLEMENTED_ONLY` | The shared worker validates configuration before claim, caps batches at ten, loads private data server-side, renders escaped templates, calls claim/send/finish, and reduces failures safely. Focused fake tests cover accepted, timeout, retryable, permanent, duplicate, and attempt-three dead-letter paths; the schedule is not deployed and live attempt tables remain empty. |
| Owner retry control | Yes* | RPC plus local UI | Email provider | No | No | Authorized retry-cycle reset | No | Local owner operations | Safe rejection | Owner replay contract | No | `IMPLEMENTED_ONLY` | The local `/admin` UI runs the server `whoami` check, exposes retry only for dead-letter/cancelled non-legacy events, and invokes `owner_retry_notification` without direct browser table writes. The live RPC rechecks `is_owner()` and rejects legacy events. No eligible live event has been replayed, and this UI is not deployed. |
| Provider message/delivery evidence | Yes* | Adapter, signed route, schema/RPC, local owner UI | Email provider | No | No | Provider ID, attempt, callback, accepted/delivered fields | No | Local owner operations | Durable safe state | Provider-event dedupe | No | `IMPLEMENTED_ONLY` | The local adapter separates provider acceptance from delivery; the signed route admits only verified event metadata; and the local owner UI displays attempt time/status, provider message ID, safe error, acceptance, and delivered callback as distinct fields. The route now fails closed if its raw request body is unreadable before verification or storage, and owner/webhook contract tests are included in the 156-test aggregate suite. Fixtures pass, but provider/attempt tables remain empty: no secret, registration, connection, accepted event, or delivered callback exists live. |
| AI triage adapter | Yes* | Yes, local repair | MiniMax M3 | Runtime records `MiniMax-M3`; environment names cannot be read back through Netlify CLI | MiniMax invocation reaches an invalid-output failure | Persists inquiry before triage; safe attempt metadata columns live | Two owner-authorized controlled inquiries | Local owner view; deployed owner view lacks triage details | Safe local codes for timeout/rejection/auth/rate-limit/5xx/network/invalid output | No | No | `FAILED_LIVE_LOCAL_REPAIR_READY` | The latest controlled production POST returned HTTP 201 and persisted `provider_error` / `AI triage returned an invalid result` with model `MiniMax-M3`; provider/error/attempt fields remained null. Netlify verifies the latest production build is deploy `6a786d2b0207453cc9541555` of pre-repair commit `daafc01`, not `3faadd3`. The repair has injected timeout, unavailable, and invalid-output proof, safe durable metadata, and local owner visibility; it must be the exact deployment artifact before live success can be claimed. |
| Provider-backed website AI assistant | No; deterministic guide is disclosed | No | AI provider | No verified configuration | No | No | No | No | Deterministic fallback only | No | No | `INTENTIONALLY_DEFERRED_NOT_PUBLICLY_CLAIMED` | The corner assistant is explicitly a pre-written decision tree and never calls `/api/chat`. The public AI Receptionist copy now identifies it as a deterministic guide, not an AI chat, phone, or DM assistant. |
| Deterministic website workflow guide | Yes, accurately labelled | Yes | No | N/A | N/A | No | No | Visitor observes | N/A | N/A | Yes | `VERIFIED_LIVE` | `SiteAssistant` labels itself “not a live AI” and gives pre-written navigation/pricing guidance. |
| AI Receptionist local intake/handoff/call log | No; future requirements disclosed | UI copy only | AI + telephony/messaging for live channels | No | No | No call/conversation log | No | No | No | No | No | `INTENTIONALLY_DEFERRED_NOT_PUBLICLY_CLAIMED` | Public copy now calls this a future, separately approved build and states that no AI receptionist is operating on the site. It lists disclosure, human-handoff, audit, provider, and retention requirements without claiming implementation. |
| Live voice / telephony / messaging | Disclosed as absent | No | Voice/telephony/messaging providers | No | No | No | No | No | No | No | No | `INTENTIONALLY_DEFERRED_NOT_PUBLICLY_CLAIMED` | The site explicitly states that no telephony, voice, messaging, or model provider is configured, and requires approved provider/callback evidence before a live claim. |
| Owner authentication | UI/internal | Yes, local explicit-token hardening | Supabase Auth | Yes | Yes | Auth state | Auth session | Owner | Safe 401/403 | Provider-managed | Yes, owner-attested | `VERIFIED_LIVE` | One confirmed authorized-email user exists and the deployed `whoami` rejects anonymous access. Local commit `55e21ee` replaces indirect custom-header reliance with bounded one-token parsing and explicit `auth.getUser(token)` validation in both owner-only routes. The corrected route remains undeployed, so a fresh authenticated owner-session proof is still required before stronger current-runtime claims. |
| Owner MFA | Internally required and previously claimed complete | No application enforcement | Supabase Auth | No verified factor | No | Auth state | Auth challenge | Owner | Partial | Provider-managed | No | `FAILED_LIVE` | Live `auth.mfa_factors` readback reports zero verified factors for the authorized user, contradicting the previous MFA-complete attestation. |
| Single-owner RLS authorization | Yes | Yes | Supabase | Yes | Yes | Controls private rows | No | Owner | Safe RLS denial | N/A | Yes, policy/ACL readback | `VERIFIED_LIVE` | Live migration `20260809101715` makes `is_owner()` require both `app_metadata.role=owner` and `email=ironwakee@gmail.com`. All six private tables now have one authenticated policy using the canonical predicate for `USING` and `WITH CHECK`. Live owner-session/MFA verification remains a separate failed capability. |
| Restricted privileged RPC surface | Internally required | Yes | Supabase | Yes | Yes | Server intake/controlled retention | No browser-side effect | No | Live privilege/advisor readback | N/A | Yes | `VERIFIED_LIVE` | Live ACL readback denies `anon` and `authenticated` execution on `submit_audit_inquiry`, `anonymize_expired_inquiries`, and `rls_auto_enable`; service role retains only intake/retention execution and `postgres` retains event-trigger execution. The prior three advisor findings are gone. |
| Owner CRM list/search/sort/export | Yes* | Yes | Supabase | Yes | Yes | Reads private rows | Browser CSV | Owner | Generic read error | Manual refresh/sign-in | Login/list only | `CONNECTED_NOT_VERIFIED` | Owner login/list was attested. Search/sort/export are client code over at most 25 inquiry rows; no current interaction evidence proves all three. |
| Owner-visible source/triage/booking/notification detail | Yes | Partial local UI | Supabase | Yes | Partial | Reads inquiries/tasks/notes/audit/outbox/attempts | No | Partial | Partial | Notification replay only | No | `IMPLEMENTED_ONLY` | The local `/owner` view now shows source, booking status, request summary, triage, provider/error metadata, next action, due date, linked task, owner notes, activity timeline, and a bounded open/overdue task queue. The separate local `/admin` view covers queue/attempt/provider delivery state and eligible notification replay. Neither private UI is deployed or owner-session verified. |
| Lead-stage updates | Yes, local owner UI | Yes | Supabase | Yes | Yes | Inquiry stage + audit metadata | No | Local owner control | Safe RPC/authorization error | No | No | `IMPLEMENTED_ONLY` | Live `owner_update_inquiry_stage` rechecks the canonical owner predicate, validates the allowed stage set, updates one inquiry, and records the prior/new stage in an audit event. The local owner UI invokes only that RPC; no deployed owner-session interaction is claimed. |
| Lead assignment | No; public copy qualified | No | No | N/A | N/A | No assignee field | No | No | No | No | No | `INTENTIONALLY_DEFERRED_NOT_PUBLICLY_CLAIMED` | The product has a single designated owner but no multi-user assignment model. Public copy now says named assignment remains unfinished rather than representing a task due date as an assignee. |
| Next action and follow-up task creation | Yes | Yes, local UI | Supabase | Yes | Yes | Durable task plus `inquiries.next_action` / `due_at` | No | Local owner view | Safe task-query/RPC result | No | No | `IMPLEMENTED_ONLY` | Live forward migration backfilled 37 persisted inquiries with an open-task due date and next action. New audit/booking intake writes the same due timestamp to inquiry and task. The local owner UI reads the linked open task; deployment and owner-session proof remain pending. |
| Follow-up engine / escalation / completion | Yes | Yes, minimal local | Notification provider for alerts | No | No | Open task, due state, owner completion audit | No | Local owner completion control | Safe `false`/authorization error | No provider reminder | No | `IMPLEMENTED_ONLY` | Live `owner_complete_task` is security-invoker, rechecks the canonical owner predicate, updates one incomplete task, updates the inquiry’s next action, and writes `follow_up_task_completed`. Anonymous execution is denied. There is no deployed owner interaction, provider reminder/escalation, or customer follow-up message. |
| Owner notes and activity timeline | Yes, local UI | Supabase | Yes | Yes | Owner note + metadata-only audit event | No | Local owner view | Safe RPC/authorization error | No | No | `IMPLEMENTED_ONLY` | Live `owner_notes` is RLS-protected; only the canonical owner can read it. Local `/owner` reads notes and audit metadata, and records a note only through `owner_add_inquiry_note`, which rechecks the owner predicate and stores only `note_id` in the audit event. No deployed owner-session interaction is claimed. |
| Retention anonymization | Internally required | Function plus local read-only owner status | Supabase service-role executor | Function secured; no schedule | No executor | Can anonymize only through service role | Local read-only status | No deletion action | No execution telemetry | No | No | `PARTIAL_LIVE_BOUNDARY_LOCAL_STATUS` | Live readback finds zero currently expired pending inquiries, two historical anonymized inquiries, no `pg_cron` relation, and a security-definer function executable by `service_role` only (not anonymous/authenticated). The local owner view renders retention/anonymized state but cannot start anonymization. No schedule, bounded worker, owner deletion request, restoration proof, or lifecycle evidence is wired. |
| Data export | Internally required | Bounded local owner snapshot | Supabase | Yes | Yes | Reads up to 1,000 rows from each CRM collection | Local browser JSON attachment | Authorized owner after server check | Safe 401/403/503 response | Manual | No | `IMPLEMENTED_ONLY` | Local `/api/owner/export` validates the bearer session with Supabase Auth, then relies on owner RLS to build a non-cached attachment across inquiries, contacts, consents, tasks, outbox state, attempts, provider events, owner notes, and audit history. It cannot mutate data and is explicitly bounded per collection; it is not deployed or owner-session exercised. |
| Deletion / consent withdrawal | Internally required | Consent withdrawal local control; deletion request absent | Supabase | Consent RPC live; deletion control absent | No deletion executor | Contact consent withdrawal cancels only unsent customer intents | Local owner control pending deployment | Explicit confirmation | Safe RPC failure | Consent withdrawal idempotent | No | `PARTIAL_LIVE_SCHEMA_LOCAL_UI` | Live `owner_withdraw_inquiry_consent` is security-definer but authenticated-only and internally rechecks the canonical owner. It records withdrawal, clears follow-up, cancels only queued/retry-scheduled customer notifications, and writes audit metadata. No invocation was performed. A deletion request, legal retention decision, anonymization schedule, recovery proof, and deployed owner-session evidence remain absent. |
| Notification operations dashboard | Yes | Full local notification view | Supabase | Yes | Yes | Reads owner-RLS outbox/inquiry/attempt metadata; retry RPC | No provider side effect | Authorized owner after server check | Safe UI/RPC errors | Eligible terminal non-legacy replay | No | `IMPLEMENTED_ONLY` | The local page fails closed through server `whoami`, then relies on owner RLS to join inquiry, outbox, and attempt metadata. Its owner-only readiness route exposes only configured/not-configured plus a safe code, so queued events never imply sending or delivery. It distinguishes accepted from delivered and offers only RPC-backed eligible replay. The UI is not deployed and no live owner interaction/replay was run. |
| Sentry error monitoring | Internally required | Yes, inert | Sentry | No verified DSN | No evidence | No | Error event if enabled | No | Console only | Provider-managed | No | `IMPLEMENTED_ONLY` | Sentry initializes only with a DSN. Netlify reports no configured production environment names and there is no received-event proof. |
| Server observability/correlation | Yes* | Safe notification ledger/callback path plus console | Optional monitoring | No | No | Durable notification attempt/provider-event schema | Logs only | Partial | Safe notification codes; coarse AI errors | Notification DB contract | No | `IMPLEMENTED_ONLY` | Notification attempt/callback correlation can be stored by event and inquiry, and local callback responses expose no payload or secret details. No live worker/provider event exists, AI lacks an attempt ledger, and no monitoring alert or deployed log-redaction readback exists. |
| Analytics | Not publicly claimed | No | Optional analytics provider | No | No | No | No | No | No | N/A | No | `INTENTIONALLY_DEFERRED_NOT_PUBLICLY_CLAIMED` | Privacy-first analytics remains deferred. |
| Payments | Explicitly excluded | No | Payment provider | No | No | No | No | No | No | N/A | No | `INTENTIONALLY_DEFERRED_NOT_PUBLICLY_CLAIMED` | Pricing is informational; no checkout or payment-success state is exposed. |
| Social profile connection | Structured-data/public-profile claim | No operational workflow | Instagram | Historical connection only | Not independently reverified here | No | Profile read/publish would be external | Owner | Partial | N/A | No | `CONNECTED_NOT_VERIFIED` | Production JSON-LD publishes the Instagram URL; this run did not re-authenticate or mutate the account. No social-to-CRM workflow exists. |

## Live database/provider facts controlling the current partial state

1. The missing local `006` migration is superseded live by forward migration `20260809101715`; all six owner policies now use the canonical role-plus-email predicate.
2. The unintended browser-role execution of the three privileged functions is remediated and the corresponding Supabase advisor findings are gone.
3. All 36 historical notification intents are now safely `cancelled` as target `legacy`; notification-attempt and provider-event tables are empty, so no provider send, acceptance, or delivery has happened.
4. AI triage has zero completed rows. The second controlled production test returned HTTP 201 but persisted `provider_error` / invalid output with `MiniMax-M3` and null safe attempt metadata, so MiniMax runtime reachability is not structured-triage success.
5. The authorized owner user is confirmed, but has zero verified MFA factors in live Supabase.
6. The owner dashboard cannot display inquiry `source`, so prior dashboard attestation of `source = website_booking` is not supported by the deployed UI source.
7. Netlify serves the deployed site, but the project exposes no Git-linked repository settings or configured environment-variable names through the current CLI readback; future reproducible deployment is unproved.

## Immediate P0 remediation order

1. `COMPLETED 2026-08-09` — revoke unintended privileged RPC execution and apply the single-owner predicate consistently.
2. `LOCAL EMAIL OPERATIONS VERIFIED 2026-08-09` — provider-neutral templates, Resend adapter, bounded worker, priority hook, undeployed schedule entrypoint, signed raw-body webhook, and server-authorized owner attempt/failure/replay UI pass no-network/local tests.
3. Connect the selected zero-cost transactional-email provider only after account/domain/G4/G5 approval; then run one controlled delivery proof.
4. Repair AI provider configuration or remove runtime AI claims; record provider/model and attempt status durably.
5. Implement booking lifecycle, task ownership, follow-up operations, and owner-visible failure/detail views before restoring operational public claims.
6. Remove or qualify every public sentence that currently describes missing behavior as built, operating, automatic, retried, assigned, or end-to-end.

## Verdict

`IRONWAKE_REAL_PRODUCT_COMPLETION = PARTIAL`

The public frontend, Supabase intake core, single-owner RLS predicate, privileged-function boundary, and durable notification state machine are live; the email adapter/worker/signed webhook/owner notification operations are local-only. The operational business system is not complete: provider-backed email execution, registered and exercised signed callbacks, retry/delivery evidence, AI, booking lifecycle, broader follow-up/owner operations, and MFA are still missing, implemented-only, unverified, or failed live.
