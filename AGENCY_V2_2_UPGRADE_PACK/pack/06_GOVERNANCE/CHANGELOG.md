# CHANGELOG — production-hardening audit pass (2026-07-10)
Every change below is ADDITIVE. Zero lines of pre-existing content were rewritten or deleted. Format: change → production justification.

## Modules (canonical, propagated to kits + skill references)
1. **39c U§35 Data Governance (retention matrix, GDPR/DPDP erasure runbook incl. hashed-suppression paradox resolution, backup + quarterly restore drill).** Justification: erasure existed only as a healthcare-overlay checkbox; UK/EU targets create the obligation for every niche. Backup/restore existed nowhere — an unrestorable client DB is an agency-ending incident. (Repo previously: no retention defaults, no erasure procedure, no backups.)
2. **39c U§36 Async Reliability Standard (uniform retry+jitter policy, dead_letters table + replay, outbox pattern, 5-min replay window note, Gate R resilience matrix R1–R7 with measurable pass/fail).** Justification: retries/idempotency existed as per-channel fragments (WA Gate B, §25C, U§33.8) with no uniform contract, no DLQ (exhausted retries currently vanish), and no committed-row-but-send-crashed protection — the exact half-done failure T1_03's runbook fears. Phase-11 requirement for measurable criteria satisfied by Gate R.
3. **40c V§40 Call Resilience & Telephony Abuse (silence/no-input reprompt ladder, spoken recording notice, concurrency & busy policy, per-caller velocity caps + transfer allowlist, STT repair thresholds).** Justification: platform configs shipped only a 30s hard hang-up (dead air before that = perceived dead line); recording *storage* was governed (§V1) but the legally required *notice* was absent; concurrency overflow silently missed calls; nothing rate-limited the telephone surface (HTTP limiter never sees Meta/carrier traffic) — demo numbers in cold outreach are a cost-attack target; §37's DTMF fallback assumed confidence thresholds that were never defined.
4. **42c W§39 Delivery Ordering, Context & Flood Hardening (persist-then-process webhook_events, per-conversation provider-ts ordering, status monotonicity, actionable reconciliation cron; rolling-summary context management + human-takeover pack; per-contact flood coalescing with STOP bypass; 3 missing media controls completing §W4).** Justification: wamid dedup existed but out-of-order events and status races were unhandled (wrong conversation state under load); memory was last-N-turns (day-6 customer loses their day-1 quote; takeover inherits raw scrollback); inbound floods had zero cost bound (Meta's IPs bypass the HTTP limiter); §W4 had AV-scan but no MIME-by-magic-bytes, size caps, or EXIF strip (GPS leakage into lead records).

## New repo-level files
5. **06_GOVERNANCE/GOVERNANCE.md** — SSOT + propagation checklist + checksum drift detection (the repo carries each module in 3 physical copies; ungoverned = guaranteed drift); citation namespace U§/V§/W§ (three tracks collide on §22–§40 numbers); reference-resolution map (T1_01 mislabel, missing 40b, suppression_list schema drift, Flows-rotation 180d-vs-12mo contradiction resolved: 180d authoritative); deprecation table; unified rotation cadence; dual-runtime model policy (GLM-vs-Claude naming is intentional, documented).
6. **06_GOVERNANCE/CHECKSUMS.sha256** — generated manifest of every canonical + copied file; `sha256sum -c` = drift detector.
7. **07_AUDIT/AUDIT_REPORT.md** — Phase-15 deliverables 1–3, 6–10.
8. **00_MANIFEST_README.md** — appended "Hardening pass" section (pointer only; original manifest text untouched).

## Explicitly NOT added (Phase-12 filter — candidates rejected because already implemented)
Media AV-scan (§W4) · recording retention/deletion (§V1) · voice load testing (Gate F, 50 calls) · retries w/ backoff mention (WA Gate B) · template PAUSED handling (QUALITY_RATING_PLAYBOOK step 3) · ADRs (decisions.template) · replay defense duplication (wamid/reference_id dedup already neutralizes it — §36.4 documents rather than re-implements) · prompt-leakage test (injection tests 1+4 cover it) · fallback-hierarchy rewrite (grounded→clarify→escalate already consistent across V/W/widget files) · portfolio changes (P0–P6 audited: realistic, costed, gated — no fake complexity to remove, none added).
